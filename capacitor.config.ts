import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scottreinhart.twentyfortyeight',
  appName: '2048',
  webDir: 'dist'
};

export default config;
